<!-- Là, ce que j'ai fait c'est la liste des Bookings (pour les voyageurs)
- Il faut modifier l'avatar pour qu'il corresponde à l'avatar du guide.
- Il faut modifier la classe "status" avec des "if" pour que la couleur change en fonction de Accepted, Pending & Declined.
- Il faut rajouter une tab pour les Tours booked (pour les guides) avec la dropdown list pour les status -->

<div class="container">
  <% @bookings.each do |booking| %>
    <% @tours.each do |tour| %>
      <div class="message">
        <img class="avatar-large" src="https://kitt.lewagon.com/placeholder/users/gabriel-dehan">
        <div class="message-name">
          <h3><%= booking.user.first_name %></h3>
          <p><%= booking.date %></p>
        </div>
        <div class="message-body">
          <p><strong><%= tour.name %></strong></p>
          <p>
            <%= tour.description %>
          </p>
        </div>
        <div class="message-status">
          <p class="status"><%= booking.status %></p>
          <p class="price"><%= tour.price %>€</p>
        </div>
      </div>
    <% end %>
  <% end %>
</div>
