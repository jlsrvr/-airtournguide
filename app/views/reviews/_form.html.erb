<%= simple_form_for([ tour, review ], remote: true) do |form| %>
  <%= form.input :rating, collection: (1..5).to_a, label: "Rate your experience" %>
  <%= form.input :content, as: :text %>
  <%= form.button :submit %>
<% end %>
